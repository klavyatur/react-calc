import React, { useState } from 'react';
import Button from './Button';

const Calculator = () => {
  const buttons = ["%", 7, 8, 9, "x" 4, 5, 6, "-", 1, 2, 3, "+"]
  const buttonDisplay = [];

  Button.forEach(el => Button.push(<Button >))

  return (
    <div style={{border: '1px solid black' }}>
      <div>
        
        </div>

      </div>
    </div>
  )
}

export default Calculator;